{
  "scripts": {
    "build": "rimraf ./dist/ && tsc && cp manifest.json dist/manifest.json && cp inject.js dist/inject.js && cp popup.js dist/popup.js && cp popup.html dist/popup.html && node fix_imports.js",
    "watch": "rimraf ./dist/ && tsc --watch"
  },
  "devDependencies": {
    "@types/cbor": "^5.0.1",
    "@types/chrome": "^0.0.326",
    "@types/loglevel": "^1.5.4",
    "loglevel-plugin-prefix": "^0.8.4",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/cbor": "^5.0.1",
    "@types/chrome": "^0.0.326",
    "@types/loglevel": "^1.5.4",
    "loglevel-plugin-prefix": "^0.8.4",
    "typescript": "^5.8.3"
  }
}
